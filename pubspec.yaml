name: ai_art_style_transfer_demo
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.6

  tflite_flutter: ^0.10.4
  hl_image_picker: ^1.2.16
  tflite_flutter_processing: ^0.4.1
  image: ^4.2.0
  logger: ^2.3.0
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/
    - assets/models/